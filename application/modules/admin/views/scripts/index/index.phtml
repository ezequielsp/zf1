<?php
$title      = $this->escape($this->title);
$module     = $this->escape($this->module);
$controller = $this->escape($this->controller);
$resource   = $module . '/' . $controller;
?>
<div class="row">
    <div class="col-xs-12 col-md-12">
        <button 
            type="button" 
            class="btn btn-primary btn-lg js-btn-modal" 
            data-toggle="modal" 
            data-target="#myModal"
            data-modal-title="Adicionar <?php echo $title; ?>"
            data-modal-body="<?php echo $resource; ?>/add"
            data-form-success="<?php echo $resource; ?>"
        >
            <i class="icon-plus"></i> Adicionar <?php echo $title; ?>
        </button>
        <?php echo $this->paginator; ?>

        <div class="table-scrollable">
                <table class="table table-sm table-hover">
                    <thead class="thead-default">
                        <tr>
                            <th>
                                #
                            </th>
                            <th class="text-center">
                                Nome
                            </th>
                            <th class="text-center">
                                Login
                            </th>
                            <th class="text-center">
                                Perfil
                            </th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                    <?php foreach ($this->paginator as $row) : ?>
                        <?php 
                            $id   = $this->escape($row->id);
                            $edit = "{$resource}" . "/edit/id/{$id}"; 
                        ?>
                        <tr>
                            <td>
                                <?php echo $row->id; ?>
                            </td>
                            <td class="text-center">
                                <?php echo $row->name; ?>
                            </td>
                            <td class="text-center">
                                <?php echo $row->login; ?>
                            </td>
                            <td class="text-center">
                                <?php echo $row->role; ?>
                            </td>
                            <td class="text-center">
                                <button 
                                    type="button" 
                                    class="btn btn-default btn-lg js-btn-modal" 
                                    data-toggle="modal" 
                                    data-target="#myModal"
                                    data-modal-title="Editar <?php echo $title; ?>"
                                    data-modal-body="<?php echo $edit ?>"
                                    data-form-success="<?php echo $resource; ?>"
                                    data-form-edit="true" >
                                    <span class="">Editar</span>
                                </button>
                            </td>
                        </tr>
                    <?php endforeach; ?>
                    </tbody>
                </table>
        <?php echo $this->paginator; ?>
    </div>
</div>
